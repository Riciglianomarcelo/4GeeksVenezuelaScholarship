Coloca aquí la imagen real del QR de Zelle con el nombre exacto:

  zelle-qr.png

El sitio ya la referencia automáticamente en /images/zelle-qr.png
(vía la variable de entorno ZELLE_QR_PATH). Si usas otro nombre de
archivo, actualiza ZELLE_QR_PATH en tus variables de entorno de Railway.
